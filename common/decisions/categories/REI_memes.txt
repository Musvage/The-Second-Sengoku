REI_the_force_awakens = {
	
	icon = REI_memes # Fadol Ritleh

	allowed = {
		tag = REI
		REI = { is_ai = no }
		OR = {
			has_country_flag = REI_wtf
			has_country_flag = REI_tfaw
		}
	}

	picture = GFX_decision_isles_crisis
	
	priority = {
		base = 10
	}
	
	custom_icon = {
		tag = CHU
		value = chu_isles_crisis_icon_value
		desc = chu_isles_crisis_value_desc

		visible = {
			country_exists = CHU
		}
	}
	
	custom_icon = {
		tag = SHI
		value = shi_isles_crisis_icon_value
		desc = shi_isles_crisis_value_desc

		visible = {
			country_exists = SHI
		}
	}

	visible_when_empty = yes

	visible = {
		#NOT = { IMS = { has_completed_focus = IMS_? } }
		NOT = { IMS = { has_war_with = SHI } }
		NOT = { IMS = { has_war_with = CHU } }
		IMS = {
			exists = yes
			is_subject = no
		}
	}
}

