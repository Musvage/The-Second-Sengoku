#Category only allowed for the controller of Tokyo#

international_recognition = {
	controller_of_tokyo = {
		icon = generic_prepare_civil_war
		available = {
			OR = {
				has_full_control_of_state = 64 #Change that later to Tokyo's State ID
				controls_state = 64
			}
			NOT = {
				has_idea = all_internationally_recognised
			}
		}
		activation = {			
			OR = {
				has_full_control_of_state = 64 #Change that later to Tokyo's State ID
				controls_state = 64
			}
			NOT = {
				has_idea = all_internationally_recognised
			}
		}
		days_mission_timeout = 200
		highlight_states = {
			state = 64 #Change that later to Tokyo's State ID
		}
		cancel_trigger = {
			NOT = {
				has_full_control_of_state = 64 #Change that later to Tokyo's State ID				
			}
		}
		fire_only_once = yes
		is_good = yes
		timeout_effect = {
			custom_effect_tooltip = internationally_recognised_tt
			country_event = internationalrecognised.1
			set_country_flag = internationally_recognised
		}
		ai_will_do = {
			factor = 0
		}
	}
	not_controller_of_tokyo = {
		icon = generic_prepare_civil_war
		available = {
			has_idea = all_internationally_recognised		
			NOT = {
				OR = {
					has_full_control_of_state = 64 #Change that later to Tokyo's State ID
					controls_state = 64
				}
			}
		}
		activation = {
			has_idea = all_internationally_recognised		
			NOT = {
					has_full_control_of_state = 64 #Change that later to Tokyo's State ID
			}
		}
		days_mission_timeout = 200
		highlight_states = {
			state = 64 #Change that later to Tokyo's State ID
		}
		cancel_trigger = {
			has_full_control_of_state = 64 #Change that later to Tokyo's State ID				
		}
		fire_only_once = yes
		is_good = no
		timeout_effect = {
			custom_effect_tooltip = not_internationally_recognised_tt
			country_event = internationalrecognised.2
			clr_country_flag = internationally_recognised
		}
		ai_will_do = {
			factor = 0
		}
	}	
}

