world_stage_mongolia_category_first = {
	
	world_stage_mongolia_send_weapons_first = {

		icon = decision_generic_industry
		
		available = {
			has_global_flag = world_stage_theater_mongolia_is_active_first
			OR = {
				tag = FER
				tag = ROC
			}
		}
		
		visible = {
			has_global_flag = world_stage_theater_mongolia_is_active_first
			OR = {
				tag = FER
				tag = ROC
			}
		}
		
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 999 } 
		}
		
		custom_cost_text = world_stage_1000_weapons_cost
		
		days_remove = 14
		
		complete_effect = {
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = 1000
				}
			}
		}
		
		remove_effect  = {
			if = {
				limit = { tag = ROC }
				add_to_variable = {
					global.CHI_strength_mongolia_bonus = 5
					tooltip = world_stage_add_local_bonus
				}
			}
			if = {
				limit = { tag = FER }
				add_to_variable = {
					global.SOV_strength_mongolia_bonus = 5
					tooltip = world_stage_add_local_bonus
				}
			}
		}
	}
	
	world_stage_mongolia_send_forces = {

		icon = decision_generic_military
		
		available = {
			has_global_flag = world_stage_theater_mongolia_is_active_first
			OR = {
				tag = FER
				tag = ROC
			}
		}
		
		visible = {
			has_global_flag = world_stage_theater_mongolia_is_active_first
			OR = {
				tag = FER
				tag = ROC
			}
		}
		
		custom_cost_trigger = {
			OR = {
				AND = {
					tag = FER
					check_variable = { global.world_stage_power_sov > 4 }
				}
				AND = {
					tag = ROC
					check_variable = { global.world_stage_power_chi > 4 }
				}
			}
		}
		
		custom_cost_text = world_stage_5_power_cost
		
		days_remove = 14
		
		remove_effect  = {
			if = {
				limit = { tag = ROC }
				add_to_variable = {global.world_stage_power_chi = -5 }
				add_to_variable = {
					global.CHI_strength_mongolia = 5
					tooltip = world_stage_add_local_power
				}
				
			}
			if = {
				limit = { tag = FER }
				add_to_variable = {global.world_stage_power_sov = -5 }
				add_to_variable = {
					global.SOV_strength_mongolia = 5
					tooltip = world_stage_add_local_power
				}
			}
		}
	}

	world_stage_mongolia_send_adviser = {

		icon = decision_generic_army_support
		
		available = {
			has_global_flag = world_stage_theater_mongolia_is_active_first
			OR = {
				tag = FER
				tag = ROC
			}
		}
		
		visible = {
			has_global_flag = world_stage_theater_mongolia_is_active_first
			OR = {
				tag = FER
				tag = ROC
			}
		}
		
		custom_cost_trigger = {
			command_power > 24.99
		}
		
		custom_cost_text = world_stage_25_cp_cost
		
		days_remove = 14
		
		complete_effect = {
			hidden_effect = { add_command_power = -25 }
		}
		
		remove_effect  = {
			if = {
				limit = { tag = ROC }
				add_to_variable = {
					global.CHI_strength_mongolia_bonus = 5
					tooltip = world_stage_add_local_bonus
				}
			}
			if = {
				limit = { tag = FER }
				add_to_variable = {
					global.SOV_strength_mongolia_bonus = 5
					tooltip = world_stage_add_local_bonus
				}
			}
		}
	}
}

world_stage_spanish_cw_category = {
	
	world_stage_spanish_cw_send_forces = {

		icon = decision_generic_military
		
		available = {
			has_global_flag = world_stage_theater_spanish_cw_is_active
			OR = {
				has_government = fascism
				has_government = communism
			}
		}
		
		visible = {
			has_global_flag = world_stage_theater_spanish_cw_is_active
			OR = { tag = REI tag = FER }
			OR = {
				has_government = fascism
				has_government = communism
			}
			OR = {
				has_completed_focus = REI_help_spain
			}
		}
		
		custom_cost_trigger = {
			has_world_stage_power_SELF_5 = yes
		}
		
		custom_cost_text = world_stage_5_power_cost
		
		days_remove = 30

		fire_only_once = yes
		
		remove_effect  = {
			if = {
				limit = { has_government = fascism }
				custom_effect_tooltip = country_name_falange
				add_to_variable = {
					global.FAL_strength_spain = 5
					tooltip = world_stage_add_local_power
				}
			}
			if = {
				limit = { has_government = communism }
				custom_effect_tooltip = country_name_republica
				add_to_variable = {
					global.REP_strength_spain = 5
					tooltip = world_stage_add_local_power
				}
			}
			remove_world_stage_power_SELF_5 = yes
		}
	}
	
	world_stage_spanish_cw_send_weapons = {

		icon = decision_generic_industry
		
		available = {
			has_global_flag = world_stage_theater_spanish_cw_is_active
			OR = {
				has_government = fascism
				has_government = communism
			}
		}
		
		visible = {
			has_global_flag = world_stage_theater_spanish_cw_is_active
			OR = {
				has_government = fascism
				has_government = communism
			}
		}
		
		custom_cost_trigger = {
			has_equipment = { infantry_equipment > 999 } 
		}
		
		custom_cost_text = world_stage_1000_weapons_cost
		
		days_remove = 30
		days_re_enable = 150
		
		complete_effect = {
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = 1000
				}
			}
		}
		
		remove_effect  = {
			if = {
				limit = { has_government = fascism }
				add_to_variable = {
					global.FAL_strength_spain_bonus = 5
					tooltip = world_stage_add_help_fal_power
				}
			}
			if = {
				limit = { has_government = communism }
				add_to_variable = {
					global.REP_strength_spain_bonus = 5
					tooltip = world_stage_add_help_rep_power
				}
			}
		}
	}

	world_stage_spanish_cw_send_adviser = {

		icon = decision_generic_army_support
		
		available = {
			has_global_flag = world_stage_theater_spanish_cw_is_active
			OR = {
				has_government = fascism
				has_government = communism
			}
		}
		
		visible = {
			has_global_flag = world_stage_theater_spanish_cw_is_active
			OR = {
				has_government = fascism
				has_government = communism
			}
		}
		
		custom_cost_trigger = {
			command_power > 49.99
		}
		
		custom_cost_text = world_stage_50_cp_cost
		
		days_remove = 30
		days_re_enable = 150
		
		complete_effect = {
			hidden_effect = { add_command_power = -25 }
		}
		
		remove_effect  = {
			if = {
				limit = { has_government = fascism }
				add_to_variable = {
					global.FAL_strength_spain_bonus = 5
					tooltip = world_stage_add_help_fal_power
				}
			}
			if = {
				limit = { has_government = communism }
				add_to_variable = {
					global.REP_strength_spain_bonus = 5
					tooltip = world_stage_add_help_rep_power
				}
			}
		}
	}
}