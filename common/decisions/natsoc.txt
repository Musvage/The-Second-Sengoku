REI_shikoku_gambit  = {

REI_shikoku_economical_meddling = {

		allowed = {
		original_tag = REI
		}

		available = {
		original_tag = REI
		has_completed_focus = REI_abwehr
				SHI = {
		is_in_faction_with = ROOT
				}
		}

		cost = 150


		ai_will_do = {
			factor = 0
		}

		visible = {
		original_tag = REI
		}
		
	    fire_only_once = yes

		complete_effect = {
        add_ideas = REI_shikoku_economical_meddling
          }  
	   }
	   
REI_shikoku_army_meddling = {

		allowed = {
		original_tag = REI
		}

		available = {
		original_tag = REI
		has_idea = REI_shikoku_economical_meddling
				SHI = {
		is_in_faction_with = ROOT
				}
		}

		cost = 150


		ai_will_do = {
			factor = 0
		}

		visible = {
		original_tag = REI
		}
		
	    fire_only_once = yes

		complete_effect = {
       add_ideas = REI_shikoku_army_meddling
          }  
	   }
	   
REI_shikoku_political_meddling = {

		allowed = {
		original_tag = REI
		}

		available = {
		original_tag = REI
		has_idea = REI_shikoku_army_meddling
				SHI = {
		is_in_faction_with = ROOT
				}
		}

		cost = 150


		ai_will_do = {
			factor = 0
		}

		visible = {
		original_tag = REI
		}
		
	    fire_only_once = yes

		complete_effect = {
       add_ideas = REI_shikoku_political_meddling
          }  
	   }
	   
REI_sattelite_shikoku = {

		allowed = {
		original_tag = REI
		}

		available = {
		original_tag = REI
		has_idea = REI_shikoku_army_meddling
		has_idea = REI_shikoku_political_meddling
		has_idea = REI_shikoku_economical_meddling
		SHI = {
		is_in_faction_with = ROOT
				}
		}

		cost = 150


		ai_will_do = {
			factor = 0
		}

		visible = {
		original_tag = REI
		}
		
	    fire_only_once = yes

		complete_effect = {
		news_event = { days = 2 id = SSnews.17 }
        remove_ideas = REI_shikoku_army_meddling
		remove_ideas = REI_shikoku_political_meddling
		remove_ideas = REI_shikoku_economical_meddling
		if = {
				limit = {
				has_dlc = "Death or Dishonor" 
				}
				set_autonomy = { target = SHI autonomy_state = autonomy_satellite }
				}
		if = {
				limit = { 
				has_dlc = "Together for Victory"
				NOT = { has_dlc = "Death or Dishonor" } 
				}
				set_autonomy = { target = SHI autonomy_state = autonomy_dominion }
				}
		if = {
				limit = { 
				NOT = { has_dlc = "Together for Victory" }
				NOT = { has_dlc = "Death or Dishonor" }
				}
				puppet = SHI
			}
		SHI = { 
		create_country_leader = {
	name = "Chousokabe Yasuyuki"
	desc = ""
	picture = "gfx/leaders/Asia/Portrait_Asia_Generic_warlord2.dds"
	expire = "1965.1.1"
	ideology = despotism
	traits = {
		great_war_hero 
	}
}
		start_civil_war = {
			ruling_party = fascism
			ideology = neutrality
			size = 0.6
			  }
		   }
		  random_other_country = {
				limit = {
					original_tag = SHI
					has_government = neutrality
				}
			declare_war_on = { target = REI type = annex_everything }
			}
        }  
	}
}
	  
colony_affairs = {

appoint_the_commissar = {

		allowed = {
		original_tag = REI

		}

		available = {
		original_tag = REI
		NOT = {has_completed_focus = REI_reign_of_terror }
		NOT = {has_completed_focus = REI_wehrmacht_primacy }
		}

		cost = 0


		ai_will_do = {
			factor = 100
		}

		visible = {
		original_tag = REI
		}
		
	    fire_only_once = yes

		complete_effect = {
         country_event = cities.40
          }  
		}	
      }		