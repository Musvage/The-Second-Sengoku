political_actions = {

democratic_purge = {

		allowed = {
		original_tag = SHI
		}

		available = {
		original_tag = SHI
		OR = {
        has_completed_focus = SHI_tohokai_takeover
        has_war_support > 0.45 }
		}

		cost = 50


		ai_will_do = {
			factor = 0
		}

		visible = {
		original_tag = SHI
		}
		
		fire_only_once = yes

		complete_effect = {
         country_event = shikoku.8
		 add_political_power = -100
		 add_stability = -0.1
          }  
	   }
	}