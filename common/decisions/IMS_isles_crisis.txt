IMS_isles_crisis_chu = {
	
	IMS_demand_compliance_chu = {

		icon = chu_trade_unions_demand

		allowed = {
			tag = CHU
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_complience_demanded }
		}

		days_remove = 7
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 10

		fire_only_once = no

		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			add_to_variable = { var = chu_isles_crisis_power value = 5 }
			set_country_flag = ims_complience_demanded
			hidden_effect = {
				country_event = { id = ims_crisis.304 days = 28 }
			}
		}
	}
	
	IMS_withdraw_chu = {

		icon = chu_trade_unions_demand

		allowed = {
			tag = CHU
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_complience_demanded }
		}

		days_remove = 10
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 0

		fire_only_once = no

		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			add_to_variable = { var = chu_isles_crisis_power value = -100 }
			set_country_flag = ims_withdraw
			26 = { remove_claim_by = CHU }
			country_event = { id = ims_crisis.305 days = 1 }
			hidden_effect = {
				SHI = {
					annex_country = {
						target = IMS
					}
				}
			}
		}
	}
	
	IMS_prevent_annex_chu = {

		icon = shi_trade_unions_demand

		allowed = {
			tag = CHU
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_prevent }
			has_country_flag = ims_prevent_annex_tt_chu_trigger
			
		}

		days_remove = 3
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 0

		fire_only_once = yes
		
		war_with_target_on_remove = yes
		
		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			custom_effect_tooltip = ims_prevent_annex_tt_chu
			add_to_variable = { var = shi_isles_crisis_power value = -100 }
			set_country_flag = ims_prevent
			hidden_effect = {
				country_event = { id = ims_crisis.306 days = 1 }
			}
		}
	}
	
	IMS_annex_chu = {

		icon = shi_trade_unions_demand

		allowed = {
			tag = CHU
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_prevent }
			has_country_flag = ims_annex_tt_chu_trigger
			
		}

		days_remove = 5
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 0

		fire_only_once = yes
		
		war_with_target_on_remove = yes
		
		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			annex_country = {
				target = IMS
				transfer_troops = yes
			}
			add_to_variable = { var = chu_isles_crisis_power value = 100 }
			set_country_flag = ims_annex
			hidden_effect = {
				country_event = { id = ims_crisis.307 days = 1 }
			}
		}
	}
}

IMS_isles_crisis_shi = {

	IMS_demand_compliance_shi = {

		icon = chu_trade_unions_demand

		allowed = {
			tag = SHI
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_withdraw }
		}

		days_remove = 7
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 10

		fire_only_once = no

		ai_will_do = {
			factor = 0.01
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			add_to_variable = { var = shi_isles_crisis_power value = 5 }
			set_country_flag = ims_complience_demanded
			hidden_effect = {
				country_event = { id = ims_crisis.303 days = 28}
			}
			
		}
	}
	
	IMS_withdraw_shi = {

		icon = shi_trade_unions_demand

		allowed = {
			tag = SHI
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_complience_demanded }
		}

		days_remove = 10
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 0

		fire_only_once = no

		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			add_to_variable = { var = shi_isles_crisis_power value = -100 }
			set_country_flag = ims_withdraw
			26 = { remove_claim_by = SHI }
			country_event = { id = ims_crisis.305 days = 1 }
			hidden_effect = {
				CHU = { 
					annex_country = {
						target = IMS
					} 
				}
			}
		}
	}
	
	IMS_prevent_annex_shi = {

		icon = shi_trade_unions_demand

		allowed = {
			tag = SHI
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_prevent }
			has_country_flag = ims_prevent_annex_tt_shi_trigger
			
		}

		days_remove = 3
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 0

		fire_only_once = yes
		
		war_with_target_on_remove = yes
		
		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			custom_effect_tooltip = ims_prevent_annex_tt_chu
			add_to_variable = { var = shi_isles_crisis_power value = -100 }
			set_country_flag = ims_prevent
			hidden_effect = {
				country_event = { id = ims_crisis.306 days = 1 }
			}
		}
	}
	
	IMS_annex_shi = {

		icon = shi_trade_unions_demand

		allowed = {
			tag = SHI
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_prevent }
			has_country_flag = ims_annex_tt_shi_trigger
		}

		days_remove = 5
		
		targets = { IMS }

		target_trigger = {
			FROM = {
				tag = IMS
			}
		}

		cost = 0

		fire_only_once = yes
		
		war_with_target_on_remove = yes
		
		ai_will_do = {
			factor = 0.03
			modifier = {
				
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			annex_country = {
				target = IMS
				transfer_troops = yes
			}
			add_to_variable = { var = shi_isles_crisis_power value = 100 }
			set_country_flag = ims_annex
			hidden_effect = {
				country_event = { id = ims_crisis.307 days = 1 }
			}
		}
	}
}

IMS_isles_crisis_ims = {

	IMS_talk_chu = {

		icon = chu_trade_unions_support

		allowed = {
			tag = IMS
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } } IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_talk_chu_sent }
			
		}

		days_remove = 7
		
		targets = { CHU }

		target_trigger = {
			FROM = {
				tag = CHU
			}
		}
		
		#days_re_enable = 14
		
		cost = 10

		#fire_only_once = no

		ai_will_do = {
			factor = 0.1
			modifier = {
	
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			custom_effect_tooltip = ims_talk_tt
			add_to_variable = { var = chu_isles_crisis_power value = 3 }
			set_country_flag = ims_talk_chu_sent
			hidden_effect = {
					country_event = { id = ims_crisis.301 days = 14 }
			}
		}
	}
	IMS_talk_shi = {

		icon = chu_trade_unions_support

		allowed = {
			tag = IMS
		}

		available = { IMS = { OR = { has_completed_focus = IMS_join_junta has_completed_focus = IMS_join_black_dragon } }
			has_completed_focus = IMS_join_black_dragon
			country_exists = IMS
			IMS = { has_government = neutrality }
			NOT = { has_country_flag = ims_talk_shi_sent }
		}

		days_remove = 7
		
		targets = { SHI }

		target_trigger = {
			FROM = {
				tag = SHI
			}
		}

		# days_re_enable = 14

		cost = 10

		#fire_only_once = no

		ai_will_do = {
			factor = 0.1
			modifier = {
	
			}
			
		}

		complete_effect = {
		}

		remove_effect = {
			custom_effect_tooltip = ims_talk_tt
			add_to_variable = { var = shi_isles_crisis_power value = 3 }
			set_country_flag = ims_talk_shi_sent
			hidden_effect = {
				country_event = { id = ims_crisis.302 days = 14 }
			}
		}
	}
	
}