on_actions = {
	on_war = {
		effect = {
			if = {
				limit = {
					tag = SHO
					has_country_flag = ai_path_army_reform
					NOT = { has_completed_focus = SHO_army_reform_plan }
				}
				clr_country_flag = ai_path_army_reform
				set_country_flag = ai_path_army_purge
			}
		}
	}
}