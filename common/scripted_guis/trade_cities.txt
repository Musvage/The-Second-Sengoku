scripted_gui = {

	trc_button_open = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "trc_button_open"

		visible = {
			is_ai = no
			#OR = {
			#	NOT = { has_variable = trc_button }
			#	check_variable = { trc_button = 1 }
			#}
		}
		
		effects = {
			trc_button_open_click = { 
				if = {
					limit = { has_variable = trc_button }
					clear_variable = trc_button
				}
				else = {
					set_variable = { trc_button = 1 }
				}
				
				
			}
		}

		properties = {
			#trc_button_open = {
			#	frame = trc_button
			#}
		}
	}

	trade_cities_main = {
		context_type = player_context
		#parent_window_token = politics_tab
		window_name = "trade_cities_main"

		visible = {
			is_ai = no
			check_variable = { trc_button = 2 }
		}
		
		effects = {
			
		}

		properties = {
			
		}
	}

	trade_cities_main = {
		context_type = player_context
		#parent_window_token = politics_tab
		window_name = "trc_map_states"

		visible = {
			is_ai = no
			check_variable = { trc_button = 2 }
		}
		
		effects = {
			
		}

		properties = {
			
		}
	}
	
}

