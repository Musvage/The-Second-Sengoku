scripted_gui = {

	trc_button_open = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "trc_button_open"

		visible = {
			is_ai = no
			OR = {
				NOT = { has_variable = trc_button }
				check_variable = { trc_button = 1 }
			}
		}
		
		effects = {
			trc_button_open_click = { 
				
				set_variable = { trc_button = 2 }
				set_variable = { trade_cities_main_gui = 2 }
				
			}
		}

		properties = {
			trc_button_open = {
				frame = trc_button
			}
		}
	}

	trc_button_close = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "trc_button_close"

		visible = {
			is_ai = no
			check_variable = { trc_button = 2 }
		}
		
		effects = {
			trc_button_close_click = { 
				
				set_variable = { trc_button = 1 }
				set_variable = { trade_cities_main_gui = 2 }
			}
		}

		properties = {
			trc_button_close = {
				frame = trc_button
			}
		}
	}

	trade_cities_main = {
		context_type = player_context
		#parent_window_token = politics_tab
		window_name = "trade_cities_main"

		visible = {
			is_ai = no
			check_variable = { trc_button = 2 }
		}
		
		effects = {
			
		}

		properties = {
			
		}
	}
	
}

