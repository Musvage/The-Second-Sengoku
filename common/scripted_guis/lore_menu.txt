scripted_gui = {

	lore_button_open = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "lore_button_container"

		visible = {
			is_ai = no
			OR = {
				NOT = { has_variable = trc_button }
				check_variable = { trc_button = 1 }
			}
		}
		
		effects = {
			lore_button_open_click = { 
				
				set_variable = { lore_button = 2 }
				set_variable = { lore_main_gui = 2 }

				set_variable = { show_SS_trade = 0 }
				set_variable = { show_SS_religion = 0 }
				set_variable = { show_SS_unification = 0 }
			}
		}

		properties = {
			#trc_button_open = {
			#	frame = trc_button
			#}
		}
	}

	lore_main = {
		context_type = player_context
		parent_window_token = politics_tab
		window_name = "lore_main"

		visible = {
			is_ai = no
			check_variable = { lore_button = 2 }
		}
		
		effects = {
			
		}

		properties = {
			
		}
	}

	
}

